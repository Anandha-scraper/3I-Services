{
  "name": "3i-services",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
